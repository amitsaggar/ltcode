var RabinKarpSearch = function(text, pattern) {
  let prime = 101;
  let m = pattern.length,
      n = text.length;
  var patternHash = creatHash(pattern, m-1),
      texthash = creatHash(text, m-1);

  for(i = 1; i < n-m+2; i++) {

  }

  function creatHash(string, end) {
    for 0 to end {

    }
  }

  function checkEqual(Text, start1, end1, pattern, start2, end2 ) {

  }

  function reCalcHash() {

  }
}