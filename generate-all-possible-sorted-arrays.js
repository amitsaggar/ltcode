/*
A = {10, 15, 25}
B = {1, 5, 20, 30}
*/
var sortedArrays = function(a, b) {
  let flag = true;
  let result = [];
  
  function RecFun(a, b, result , flag) {
    if(flag) {
      arr = a;
    } else {
      arr = b;
    }
    binSearch(arr, ele);
  }
};